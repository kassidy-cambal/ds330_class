"""A tensor is a lot like matrix, except that it can be in higher dimensions.
    But wait, you say, can't a matrix have higher dimensions?
    Yes.

    I think of a tensor as an n-dimensional array.

    Joel Grus does an excellent job introducing this:
    link on arthurs github"""

from numpy import ndarray as Tensor # only importing a part from numpy
